# Escribir un programa que genera la matriz transpuesta de una matriz de 3 filas y 4 columnas.
# La matriz transpuesta de una matriz M(m,n) se obtiene intercambiando filas por columnas y
# viceversa; el resultado se tiene que almacenar en una nueva matriz M_TRANS(n,m).